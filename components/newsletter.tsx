'use client';

import React from 'react';
import { Button } from "@/components/ui/button"
import { Input } from "@/components/ui/input"

/*
export default function Newsletter() {
  const handleSubmit = (e: React.FormEvent) => {
    e.preventDefault();
    // Add newsletter subscription logic here
  };

  return (
    <section className="py-20 bg-gray-900 text-white">
      <div className="container max-w-xl text-center">
        <h2 className="mb-8 text-2xl font-bold">Stay in touch!</h2>
        <p className="mb-8">SIGN UP FOR NEWS, ANNOUNCEMENTS, AND SEASONAL DRINK RECIPES</p>
        <form onSubmit={handleSubmit} className="flex gap-4 justify-center">
          <Input
            type="email"
            placeholder="Enter your email"
            className="w-64 bg-white text-black"
          />
          <Button variant="secondary">Sign Up</Button>
        </form>
      </div>
    </section>
  );
}
*/
